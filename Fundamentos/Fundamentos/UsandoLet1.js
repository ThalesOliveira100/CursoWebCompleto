var numero = 1;
{
    let numero = 2;
    console.log("dentro = ", numero);
}
console.log("fora = ", numero);

// Variáveis definidas com VAR tem escopo global e de função
// Variáveis definidsa com LET tem escopo global, de função e de bloco.
